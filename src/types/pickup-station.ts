export interface PickupStation {
  name: string;
  week: string;         // 👈 change here
  weekend: string;      // 👈 change here
  number: string;
  address: string;
  state: string;
  email: string;
  landmark: string;
  latitude: number;
  longitude: number;
}
